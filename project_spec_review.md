# Project Spec 검토 보고서
## EDA 결과 기반 `project_spec.md` 적절성 검증

---

## 1. 종속변수(Learning Poverty) 적절성 판단

### ✅ 적절하다고 판단하는 근거

| # | 근거 | EDA 데이터 뒷받침 |
|---|------|-------------------|
| 1 | **충분한 국가 커버리지** | 125개국 데이터 보유 → spec의 "약 100여 개국" 목표 초과 달성 |
| 2 | **명확한 분포 특성** | 평균 33.2%, 중앙값 18.6%로 충분한 변동성(variance) 존재 → 회귀/분류 모델링에 적합 |
| 3 | **국가 간 극명한 격차** | 잠비아 98.5% ~ 아일랜드 2.3% (96%p 차이) → 모델이 학습할 패턴이 풍부 |
| 4 | **결측값 없음** | DS-1(WB_LPGD) 자체에 결측값 0 → 종속변수 손실 없이 분석 가능 |
| 5 | **성별 세분화 가능** | Total/Male/Female 별도 제공 → 성별 하위 분석(sub-analysis) 가능 |

### ⚠️ 주의해야 할 점

| # | 이슈 | 상세 내용 | 영향도 |
|---|------|----------|--------|
| 1 | **강한 비대칭 분포** | 오른쪽 꼬리가 긴 분포(right-skewed). 선형 회귀 가정 위반 가능 | 🟡 중간 |
| 2 | **연도별 국가 구성 비일관성** | 매년 보고하는 국가가 달라짐 → 패널 데이터로 활용 시 불균형 패널 문제 | 🟡 중간 |
| 3 | **하위 지표와의 높은 상관** | 읽기미달성률(LD)·비재학률(SD)이 학습빈곤율의 **구성 요소**임 → 독립변수로 사용 시 동어반복(tautology) | 🔴 높음 |

> [!IMPORTANT]
> **학습빈곤율 = f(읽기미달성률, 비재학률)**로 정의되므로, DS-1 내의 LD, SD, LDGAP, LDSEV 등을 독립변수로 쓰면 안 됩니다. spec에서 독립변수를 DS-2(WDI)에서 가져오는 설계는 이 문제를 올바르게 피하고 있어 **적절한 설계**입니다.

---

## 2. 독립변수(Feature Variables) 적절성 검토

### F1~F8 적합성 평가

| 변수 | 적절성 | 평가 이유 |
|------|--------|----------|
| F1. GDP per capita | ✅ 적절 | EDA에서 지역별 학습빈곤율 격차가 소득 수준과 높은 연관성 보임 |
| F2. 교육 지출 비중 | ✅ 적절 | 정책 개입과 직결되는 변수. 단, 결측률 높을 수 있음 주의 |
| F3. 교사당 학생 수 | ✅ 적절 | 교육 품질의 대리 지표. EDA에서 "학교 내 학습 위기"가 핵심으로 확인됨 |
| F4. 아동 발육 부진율 | ✅ 적절 | 보건-교육 교차 변수로 독창적 |
| F5. 인터넷 사용률 | ✅ 적절 | 디지털 격차 대리 지표. 특히 코로나 이후 중요성 증가 |
| F6. 초등 순등록률 | ⚠️ 주의 | EDA에서 비재학률(SD)이 학습빈곤율의 **구성 요소**. 순등록률은 SD와 개념적 역관계 → **다중공선성 및 내생성 위험** |
| F7. 초등교육 수료율 | ⚠️ 주의 | F6과 유사한 내생성 문제. 학습빈곤율의 원인인 동시에 결과일 수 있음 |
| F8. 성별 격차 지수 | ✅ 적절 | EDA에서 성별 격차(남성 33.7% vs 여성 29.7%) 확인됨. 형평성 차원 반영에 유의미 |

> [!WARNING]
> **F6(초등 순등록률)**과 **F7(초등교육 수료율)**은 종속변수인 학습빈곤율과 **내생적(endogenous) 관계**에 있을 수 있습니다. 학습빈곤율 = 읽기미달 + 비재학이므로, 등록률/수료율이 높을수록 비재학 부분이 줄어 학습빈곤율이 기계적으로 낮아집니다. 이를 인과적으로 해석하면 오류입니다.
> 
> **권고**: F6, F7을 유지하되 VIF(다중공선성) 분석 후 하나를 제거하거나, 모델 해석 시 "기계적 관계"임을 명시하세요.

---

## 3. 분석 설계 전반 적절성 검토

### 3.1 적절한 부분 ✅

| 항목 | spec 설계 | EDA 결과와의 정합성 |
|------|----------|-------------------|
| **RQ1 (위험 요인)** | RF + SHAP + Permutation | 125개국, 8개 독립변수 → 표본 대비 변수 비율 양호 (125:8) |
| **RQ2 (클러스터링)** | K-Means + Hierarchical | EDA에서 확인된 지역별 격차 패턴이 클러스터링에 유리한 구조 |
| **RQ3 (우선순위)** | 복합 우선순위 지수 | DS-1의 LPGAP, LPSEV 지표를 심각도 산출에 활용 가능 → 스펙보다 풍부한 데이터 |
| **전처리 전략** | LAY + K-NN Imputation | DS-1 자체는 결측 없음. DS-2 병합 시에만 필요 → 현실적 전략 |
| **한계점 명시** | 5개 한계점 | L4(국가 내 격차)는 EDA에서 도시화 변수 미분화 확인으로 더욱 부각됨 |

### 3.2 개선이 필요한 부분 ⚠️

#### 이슈 1: 분포 비대칭 → 변환 전략 누락

spec에 학습빈곤율의 **분포 변환 전략**이 없습니다. EDA에서 강한 right-skew가 확인되었으므로:

```
권고: 로그 변환(log1p) 또는 Box-Cox 변환 후 모델링
      → 전처리 전략(§2.3)에 "종속변수 분포 변환" 항목 추가 필요
```

#### 이슈 2: 연도 전략 구체화 필요

spec은 "2015~2023년 최신 가용 데이터"라고 되어 있으나, EDA에서 확인된 바:
- 데이터는 2001~2023년 존재
- 연도별 보고 국가 구성이 다름
- **어떤 시점의 학습빈곤율을 종속변수로 쓸 것인지** 명확하지 않음

```
권고: "각 국가의 최신 가용 연도(Latest Available Year) 학습빈곤율" 사용 명시
      → 크로스섹션(단일 시점 국가 비교) 분석임을 spec에 명확히 기재
```

#### 이슈 3: 우선순위 지수에 DS-1 하위 지표 활용 가능

spec의 우선순위 지수에서 "학습 빈곤 심각도"를 Z-score로 산출한다고 되어 있지만, DS-1에 이미 다음 지표가 존재합니다:

| DS-1 지표 | spec 활용 가능 위치 |
|-----------|-------------------|
| SE_LPV_PRIM_LPGAP (학습빈곤 격차) | → "개선 잠재력" 직접 대체 가능 |
| SE_LPV_PRIM_LPSEV (학습빈곤 심각도) | → "학습 빈곤 심각도" 직접 대체 가능 |

```
권고: 자체 Z-score 대신 세계은행이 이미 산출한 LPGAP/LPSEV 지표를
      우선순위 지수 구성에 직접 활용하면 방법론적 신뢰도 향상
```

#### 이슈 4: 표본 크기와 모델 복잡도 균형

- 최종 분석 대상: 약 100~125개국 (크로스섹션)
- 독립변수 8개 + 맥락변수 2개
- Random Forest + SHAP은 적합하나, **과적합 위험**에 대한 언급 필요

```
권고: Leave-One-Out Cross-Validation(LOOCV) 또는
      5-fold CV 전략을 §3.2에 추가
```

---

## 4. 종합 판단

### 결론: **전체적으로 적절한 분석 설계이며, Learning Poverty는 종속변수로 적합합니다.**

```
적합도 점수: ★★★★☆ (5점 만점 중 4점)
```

| 영역 | 점수 | 평가 |
|------|------|------|
| 종속변수 선택 | ⭐⭐⭐⭐⭐ | 충분한 변동성, 국가 커버리지, 결측 없음 |
| 독립변수 설계 | ⭐⭐⭐⭐ | 대부분 적절하나 F6·F7 내생성 주의 필요 |
| 분석 방법론 | ⭐⭐⭐⭐ | 3중 검증 설계 우수, CV 전략 보완 필요 |
| 전처리 전략 | ⭐⭐⭐⭐ | 현실적이나 분포 변환 추가 필요 |
| 한계점 인식 | ⭐⭐⭐⭐⭐ | 주요 한계를 잘 파악하고 있음 |

### 최우선 수정 권고 사항 (3가지)

1. **§2.3에 종속변수 분포 변환 전략 추가** (log/Box-Cox)
2. **F6(순등록률)·F7(수료율)의 내생성 문제 언급 및 VIF 검증 계획 추가**
3. **§3.4 우선순위 지수에 DS-1의 LPGAP/LPSEV 활용 명시**
